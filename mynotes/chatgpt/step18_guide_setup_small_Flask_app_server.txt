Q  = Guide me step-by-step to build a small Flask application as the System Under Test for my Playwright GitHub project

Absolutely. 👍 For your GitHub portfolio, I think this is a much better approach than depending entirely on public demo sites.

We'll build a small but realistic Flask application as the System Under Test (SUT):

                    Your Playwright Framework
                              │
             ┌────────────────┴────────────────┐
             │                                 │
          Web UI                            REST API
             │                                 │
       Login / Users                    POST /api/login
       Dashboard                        GET  /api/users
       User creation                    POST /api/users
       User deletion                    DELETE /api/users/{id}
             │                                 │
             └────────────────┬────────────────┘
                              │
                           SQLite
                              │
                         Flask App

This will give you something real to test with Playwright Python + PyTest.

Step 1 — Create the Flask project

Inside your existing project, I recommend keeping the application separate from your automation framework:

playwright-python-framework/
│
├── app/
│   ├── app.py
│   ├── database.py
│   ├── models.py
│   ├── templates/
│   │   ├── login.html
│   │   └── users.html
│   └── static/
│       └── style.css
│
├── tests/
│   ├── ui/
│   └── api/
│
├── pages/
├── utils/
├── data/
├── reports/
├── screenshots/
├── videos/
├── traces/
│
├── conftest.py
├── requirements.txt
└── README.md

The important distinction is:

app/       = Application we're testing
tests/     = Automation testing
pages/     = Page Objects

That's a good separation to explain to a recruiter.

Step 2 — Create a virtual environment

You already have a Python environment for Playwright. You can use the same environment.

From your project directory:

python -m venv .venv

Activate it on Windows:

Git Bash
source .venv/Scripts/activate
PowerShell
.venv\Scripts\Activate.ps1

You should see:

(.venv)

in your terminal.

Step 3 — Install Flask
pip install flask

Also install SQLAlchemy. This will make the application more realistic:

pip install flask-sqlalchemy

Then update your requirements:

pip freeze > requirements.txt

Your requirements will contain things such as:

Flask
Flask-SQLAlchemy
playwright
pytest
pytest-html
Step 4 — Create app/app.py

Create:

app/app.py

Put this in it:

from flask import Flask, render_template, request, redirect, url_for, jsonify
from flask_sqlalchemy import SQLAlchemy

app = Flask(__name__)

app.config["SQLALCHEMY_DATABASE_URI"] = "sqlite:///users.db"
app.config["SQLALCHEMY_TRACK_MODIFICATIONS"] = False

db = SQLAlchemy(app)


class User(db.Model):
    id = db.Column(db.Integer, primary_key=True)
    username = db.Column(db.String(100), unique=True, nullable=False)
    password = db.Column(db.String(100), nullable=False)
    email = db.Column(db.String(150), unique=True, nullable=False)


@app.route("/")
def home():
    return redirect(url_for("login"))


@app.route("/login", methods=["GET", "POST"])
def login():

    error = None

    if request.method == "POST":

        username = request.form["username"]
        password = request.form["password"]

        user = User.query.filter_by(
            username=username,
            password=password
        ).first()

        if user:
            return redirect(url_for("users"))

        error = "Invalid username or password"

    return render_template(
        "login.html",
        error=error
    )


@app.route("/users")
def users():

    users = User.query.all()

    return render_template(
        "users.html",
        users=users
    )


# -----------------------------
# REST API
# -----------------------------

@app.route("/api/login", methods=["POST"])
def api_login():

    data = request.get_json()

    username = data.get("username")
    password = data.get("password")

    user = User.query.filter_by(
        username=username,
        password=password
    ).first()

    if not user:
        return jsonify({
            "success": False,
            "message": "Invalid username or password"
        }), 401

    return jsonify({
        "success": True,
        "message": "Login successful",
        "user": {
            "id": user.id,
            "username": user.username,
            "email": user.email
        }
    }), 200


@app.route("/api/users", methods=["GET"])
def get_users():

    users = User.query.all()

    return jsonify([
        {
            "id": user.id,
            "username": user.username,
            "email": user.email
        }
        for user in users
    ]), 200


@app.route("/api/users", methods=["POST"])
def create_user():

    data = request.get_json()

    username = data.get("username")
    password = data.get("password")
    email = data.get("email")

    if not username or not password or not email:
        return jsonify({
            "message": "Missing required fields"
        }), 400

    existing_user = User.query.filter_by(
        username=username
    ).first()

    if existing_user:
        return jsonify({
            "message": "Username already exists"
        }), 409

    user = User(
        username=username,
        password=password,
        email=email
    )

    db.session.add(user)
    db.session.commit()

    return jsonify({
        "message": "User created",
        "user": {
            "id": user.id,
            "username": user.username,
            "email": user.email
        }
    }), 201


@app.route("/api/users/<int:user_id>", methods=["DELETE"])
def delete_user(user_id):

    user = User.query.get(user_id)

    if not user:
        return jsonify({
            "message": "User not found"
        }), 404

    db.session.delete(user)
    db.session.commit()

    return jsonify({
        "message": "User deleted"
    }), 200


if __name__ == "__main__":

    with app.app_context():
        db.create_all()

    app.run(
        host="127.0.0.1",
        port=5000,
        debug=True
    )
Step 5 — Create the login page

Create:

app/templates/login.html

Put:

<!DOCTYPE html>
<html>
<head>
    <title>QA Demo Application - Login</title>
</head>

<body>

<h1>QA Demo Application</h1>

<h2>Login</h2>

<form method="POST">

    <div>
        <label for="username">Username</label>
        <input
            type="text"
            id="username"
            name="username"
        >
    </div>

    <br>

    <div>
        <label for="password">Password</label>
        <input
            type="password"
            id="password"
            name="password"
        >
    </div>

    <br>

    <button type="submit" id="login-button">
        Login
    </button>

</form>

{% if error %}

<p id="login-error">
    {{ error }}
</p>

{% endif %}

</body>
</html>

Notice that I've deliberately added IDs such as:

id="username"
id="password"
id="login-button"
id="login-error"

That's useful for your Playwright automation.

Step 6 — Create the Users page

Create:

app/templates/users.html
<!DOCTYPE html>
<html>
<head>
    <title>Users - QA Demo Application</title>
</head>

<body>

<h1>User Management</h1>

<table border="1">

    <thead>

        <tr>
            <th>ID</th>
            <th>Username</th>
            <th>Email</th>
        </tr>

    </thead>

    <tbody>

        {% for user in users %}

        <tr>

            <td>{{ user.id }}</td>

            <td>{{ user.username }}</td>

            <td>{{ user.email }}</td>

        </tr>

        {% endfor %}

    </tbody>

</table>

</body>
</html>
Step 7 — Create the database

Before starting the application, we need a test user.

The easiest way initially is to add this temporary code to app.py, just before app.run():

with app.app_context():

    db.create_all()

    if not User.query.filter_by(username="testuser").first():

        user = User(
            username="testuser",
            password="Password123",
            email="testuser@example.com"
        )

        db.session.add(user)
        db.session.commit()

So the bottom of app.py becomes:

if __name__ == "__main__":

    with app.app_context():

        db.create_all()

        if not User.query.filter_by(
            username="testuser"
        ).first():

            user = User(
                username="testuser",
                password="Password123",
                email="testuser@example.com"
            )

            db.session.add(user)
            db.session.commit()

    app.run(
        host="127.0.0.1",
        port=5000,
        debug=True
    )
Step 8 — Start the application

From the project root:

python app/app.py

You should see something similar to:

* Running on http://127.0.0.1:5000

Open:

http://127.0.0.1:5000

You should see the login page.

Use:

Username: testuser
Password: Password123

After login you should reach:

/users
Step 9 — Test the REST API manually

Before writing Playwright tests, let's make sure the API works.

You can use Postman, curl, or even your browser tools.

GET users
curl http://127.0.0.1:5000/api/users

You should get something similar to:

[
    {
        "email": "testuser@example.com",
        "id": 1,
        "username": "testuser"
    }
]
Step 10 — Test API Login

Run:

curl -X POST http://127.0.0.1:5000/api/login ^
-H "Content-Type: application/json" ^
-d "{\"username\":\"testuser\",\"password\":\"Password123\"}"

You should receive:

{
    "message": "Login successful",
    "success": true,
    "user": {
        "email": "testuser@example.com",
        "id": 1,
        "username": "testuser"
    }
}

Now we know that your SUT is working.

Step 11 — Create your Playwright API fixture

Your existing conftest.py can contain:

@pytest.fixture
def api_request():

    with sync_playwright() as p:

        request_context = p.request.new_context()

        yield request_context

        request_context.dispose()

Then create:

tests/api/test_users_api.py
Step 12 — Write your first API test
def test_get_users(api_request):

    response = api_request.get(
        "http://127.0.0.1:5000/api/users"
    )

    assert response.status == 200

    data = response.json()

    assert len(data) > 0

    assert "username" in data[0]
    assert "email" in data[0]

Run:

pytest tests/api/test_users_api.py

You should get:

1 passed

🎉 Now you're actually testing your own REST API.

Step 13 — API Login Test

Create:

tests/api/test_login_api.py
def test_valid_login(api_request):

    response = api_request.post(
        "http://127.0.0.1:5000/api/login",
        data={
            "username": "testuser",
            "password": "Password123"
        }
    )

    assert response.status == 200

    data = response.json()

    assert data["success"] is True
    assert data["user"]["username"] == "testuser"

And a negative test:

def test_invalid_login(api_request):

    response = api_request.post(
        "http://127.0.0.1:5000/api/login",
        data={
            "username": "testuser",
            "password": "WrongPassword"
        }
    )

    assert response.status == 401

    data = response.json()

    assert data["success"] is False
Step 14 — Create your UI Page Object

Now we have a proper SUT, so your existing Playwright framework can test it.

Create:

pages/login_page.py
class LoginPage:

    def __init__(self, page):

        self.page = page

        self.username = page.locator("#username")
        self.password = page.locator("#password")
        self.login_button = page.locator("#login-button")
        self.login_error = page.locator("#login-error")

    def open(self):

        self.page.goto(
            "http://127.0.0.1:5000/login"
        )

    def login(self, username, password):

        self.username.fill(username)
        self.password.fill(password)
        self.login_button.click()

    def get_error_message(self):

        return self.login_error.text_content()
Step 15 — Create the UI test

Create:

tests/ui/test_login.py
from pages.login_page import LoginPage


def test_valid_login(page):

    login_page = LoginPage(page)

    login_page.open()

    login_page.login(
        "testuser",
        "Password123"
    )

    assert "/users" in page.url

Run:

pytest tests/ui/test_login.py

You now have:

UI Test
   ↓
Playwright
   ↓
Flask Login Page
   ↓
SQLite Database
Step 16 — Add an intentionally failing test

This is useful for demonstrating your screenshot + video + trace functionality.

def test_invalid_login_failure_demo(page):

    login_page = LoginPage(page)

    login_page.open()

    login_page.login(
        "testuser",
        "WrongPassword"
    )

    # Deliberately incorrect expectation
    assert "/users" in page.url

This should fail.

Your framework should then produce:

screenshots/
    test_invalid_login_failure_demo.png

videos/
    <video>.webm

traces/
    <trace>.zip

reports/
    report.html

This gives you a very nice demonstration for your GitHub repository.

Step 17 — Add more API scenarios

Once this basic version works, I'd expand your API tests to:

tests/api/
│
├── test_login_api.py
├── test_users_api.py
├── test_create_user_api.py
└── test_delete_user_api.py

For example:

Create user
def test_create_user(api_request):

    response = api_request.post(
        "http://127.0.0.1:5000/api/users",
        data={
            "username": "automationuser",
            "password": "Password123",
            "email": "automation@example.com"
        }
    )

    assert response.status == 201

    data = response.json()

    assert data["user"]["username"] == "automationuser"

Then:

Delete user
def test_delete_user(api_request):

    response = api_request.delete(
        "http://127.0.0.1:5000/api/users/2"
    )

    assert response.status == 200

    data = response.json()

    assert data["message"] == "User deleted"
Step 18 — Your final portfolio architecture

Eventually I'd aim for this:

playwright-python-framework/
│
├── app/                         # System Under Test
│   ├── app.py
│   ├── templates/
│   │   ├── login.html
│   │   └── users.html
│   └── static/
│
├── tests/
│   ├── ui/
│   │   ├── test_login.py
│   │   └── test_users.py
│   │
│   └── api/
│       ├── test_login_api.py
│       ├── test_users_api.py
│       ├── test_create_user_api.py
│       └── test_delete_user_api.py
│
├── pages/
│   ├── login_page.py
│   └── users_page.py
│
├── utils/
│   ├── logger.py
│   ├── config.py
│   └── json_reader.py
│
├── data/
│   └── users.json
│
├── reports/
├── screenshots/
├── videos/
├── traces/
│
├── conftest.py
├── pytest.ini
├── requirements.txt
├── .gitignore
└── README.md
One important change I recommend

For a real QA project, don't keep passwords in plain text in the Flask application once we move beyond this learning/demo stage. We're doing it here only to keep the first version simple. We can later introduce environment variables and proper password hashing.

What this gives you for interviews

You can now legitimately demonstrate a complete test automation workflow:

UI

"I developed Page Object Model tests using Playwright and Python."

API

"I implemented REST API automation using Playwright's APIRequestContext."

Database

"The application uses SQLite, allowing me to validate application data."

Failure analysis

"Failed tests automatically capture screenshots, videos and Playwright traces."

Reporting

"The framework generates pytest-html execution reports."

CI/CD

"The complete suite can be executed through GitHub Actions."

That is a much stronger portfolio project than simply having a few Playwright scripts against a public demo website.

One thing I would do next

Once you get the Flask application running, don't add more features yet. First let's make sure the basic chain works:

Flask
  ↓
Web Login
  ↓
SQLite
  ↓
REST API
  ↓
Playwright UI test
  ↓
Playwright API test
  ↓
Screenshot / Video / Trace
  ↓
HTML Report

Once all of those pass, we can refactor the Flask app and your Playwright framework into a cleaner enterprise-style architecture, then add GitHub Actions so the Flask SUT automatically starts and the complete UI + API suite runs on every GitHub push.


